[project]
name = "qdrant-legal-lab"
version = "0.1.0"
description = "Qdrant legal retrieval tuning lab"
requires-python = ">=3.12"
dependencies = [
    "cryptography>=46.0",
    "qdrant-client>=1.15",
    "python-dotenv>=1.0",
]

[dependency-groups]
organizer = ["fastembed>=0.7"]

[tool.uv]
package = false
